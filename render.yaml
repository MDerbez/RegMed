services:
  - type: web
    name: jomquer
    env: python
    plan: free
    buildCommand: pip install flask gunicorn flask-session werkzeug pytz
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.4